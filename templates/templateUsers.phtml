<?php require 'templateHeader.phtml'; ?>

<div class="container my-5">
  <h2 class="text-center mb-4">Usuarios (<?php echo count($users); ?>)</h2>

  <?php if (!empty($users)): ?>
    <div class="row row-cols-1 row-cols-md-3">
      <?php foreach ($users as $user): ?>
        <div class="col">
          <div class="card shadow-sm border-0" style="width: 18rem; margin: 0 auto;">
            
            <!-- Imagen -->
            <img src="https://placehold.co/400x250?text=Usuario" 
                 class="card-img-top" 
                 alt="Imagen de <?php echo $user->name; ?>">

            <div class="card-body">
              <h5 class="card-title text-primary fw-bold">
                <?php echo $user->name; ?>
              </h5>

              <p class="card-text text-secondary">
                <?php echo $user->description ?: "Sin descripción"; ?>
              </p>
            </div>

            <ul class="list-group list-group-flush">
              <li class="list-group-item">📧 <?php echo $user->email; ?></li>
              <li class="list-group-item">🎂 <?php echo $user->age; ?></li>
            </ul>

            <div class="card-body d-flex justify-content-between">
              <a href="showUserById/<?php echo $user->id_user; ?>" class="btn btn-light">Ver detalles</a>
              <a href="updateUser/<?php echo $user->id_user; ?>" class="btn btn-light">Editar</a>
              <a href="deleteUser/<?php echo $user->id_user; ?>" class="btn btn-light">Eliminar</a>
            </div>
          </div>
        </div>
      <?php endforeach; ?>
    </div>
  <?php else: ?>
    <div class="alert alert-info text-center mt-5">
      No hay usuarios registrados todavía
    </div>
  <?php endif; ?>
</div>

<?php require 'templateFooter.phtml'; ?>
