<?php require 'templateHeader.phtml'; ?>

<div class="container my-5">
  <h2 class="text-center mb-4">Usuarios (<?php echo count($users); ?>)</h2>

  <div class="d-flex justify-content-center mt-4 mb-4">
    <a href="<?php echo BASE_URL; ?>addUser">
      <button type="button" class="btn btn-danger">Añadir usuario</button>
    </a>
  </div>

  <?php if (!empty($users)): ?>
    <div class="row row-cols-1 row-cols-md-3">
      <?php foreach ($users as $user): ?>
        <div class="col">
          <div class="card shadow-sm border-0" >

            <div class="card-body">
              <h5 class="card-title text-primary fw-bold">
                <?php echo $user->name; ?>
              </h5>

              <p class="card-text text-secondary">
                <?php echo $user->description ?: "Sin descripción"; ?>
              </p>
            </div>

            <ul class="list-group list-group-flush">
              <li class="list-group-item">📧 <?php echo $user->email; ?></li>
              <li class="list-group-item">🎂 <?php echo $user->age; ?></li>
            </ul>

            <div class="card-body d-flex justify-content-center">
              <a href="showUserById/<?php echo $user->id_user; ?>" class="btn btn-light">Ver recetas</a>
            </div>
          </div>
        </div>
      <?php endforeach; ?>
    </div>
  <?php else: ?>
    <div class="alert alert-info text-center mt-5">
      No hay usuarios registrados todavía
    </div>
  <?php endif; ?>
</div>

<?php require 'templateFooter.phtml'; ?>
